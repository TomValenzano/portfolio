---
type: diary
project: portfolio
date: 2026-07-28
---
- [17:55] (Tommaso) Aggiunto il progetto "Paninoteca da Daniele" (comelosmash.it) alla pagina /studio/: nuova card in cima alla lista `projects` di `app/pages/studio.vue` + screenshot desktop `public/img/smash.jpg` (1400×893, stesso formato delle altre card).
- [18:30] (Tommaso) Il deploy Cloudflare Pages falliva: `npm install` senza lockfile inciampava nel bug arborist di npm ("edgesOut"), poi due lockfile generati con npm 10.8.2 sono stati rifiutati (binding darwin-only, poi `npm ci` out-of-sync su commander 11/14). Fix definitivo: `package-lock.json` generato con **npm 11** in una dir pulita — passa `npm ci` di npm 10.8.2 e include i binding linux. Dettagli nel gotcha `cloudflare-pages-npm-lockfile`. Card ora live su tvalenzano.it/studio/.
- [19:50] (Tommaso) Fixato il freeze di ~4s dello scroll su /studio/ (desktop Chrome/Brave): i18n ri-naviga la rotta dopo l'hydration e con `experimental.viewTransition` la transizione resta appesa fino al timeout di 4s di Chrome che congela il rendering. Fix: `viewTransition: false` nel `definePageMeta` di studio.vue. Riprodotto e verificato con Chrome headed via puppeteer-core (rAF gap detector + hook su `startViewTransition`) — Lighthouse/headless non lo vedevano perché il blocco è sul compositor, non sul main thread. Dettagli nel gotcha `nuxt-view-transition-i18n-freeze`. Bonus: immagini progetti ricompresse a 1000px (625KB→348KB) per il caricamento lento su iOS.
